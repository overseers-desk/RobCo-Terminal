# Competitive Landscape: desktop terminal emulators

Snapshot taken August 2026. Survey scope: the retro/CRT corner (cool-retro-term and its descendants), the GPU-rendered native terminals of 2017–2026, the Electron terminals, the desktop-environment defaults, the macOS reference for tmux integration, and the two multiplexers (tmux, Zellij) that supply session survival under any terminal. The field is large and active: most mainstream entries shipped a release in 2026.

Capabilities are as their authors describe them in READMEs, docs and launch posts, not independently re-verified; cells that could not be confirmed read "unconfirmed". Star counts and release dates are approximate and omitted where unknown; stars are a weak proxy for iTerm2 and Konsole (distributed off GitHub) and for WezTerm (stale tagged releases, active nightlies). Pain numbers (P1-P13) cross-reference `use-case-survey.md`. RobCo Terminal's own coverage is not in this file; it lives in `status.md`.

## Feature matrix

Cells: Yes / No / Partial / unconfirmed, with a short note. Columns are the pains the field can supply against; P12 (maintainer conduct) and P13 (choosing) are not features and have no column.

| Tool | Platform / UI | Latency claim (P1) | GPU need, fallback (P2) | CRT / retro look (P3) | Bitmap fonts, scale (P4) | Many sessions (P5) | tmux control mode (P6) | Survival (P7) | Config model (P8) | Linux fit, packaging (P9) | Table stakes (P10) | Weight (P11) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| cool-retro-term | Qt6/QML, Linux + macOS, X11 | none | GPU via Qt scene graph; no fallback; idle draw 17–20% (PR #946) | Yes: built-in fixed CRT chain, presets; no user shaders | unconfirmed (bitmap only by recompiling, #740) | No tabs or splits (#247) | unconfirmed | No | GUI context menu, preset themes | AppImage, distro repos, Snap; Wayland crashes open (#925, #934) | Partial (qtermwidget basics) | Qt, 121 deps per a WezTerm user |
| Cathode (macOS) | Cocoa | unconfirmed | unconfirmed | Yes (third-party accounts) | unconfirmed | unconfirmed | unconfirmed | unconfirmed | GUI | N/A | unconfirmed | N/A; discontinued ~2017 |
| Ghostty | native: Metal on macOS, GTK4/OpenGL on Linux (X11 + Wayland) | "same performance category" as Alacritty | GPU required; no fallback | Partial: user GLSL `custom-shader` | Mixed: .ttf-wrapped bitmaps sometimes; blur at 125% (#1938, closed) | Tabs, splits, `goto_tab:N`; no overview | No; #1935 open (731 reactions), parser begun | Structure restore only | Text file, no GUI; per-key reload; `+show-config --default` | GTK4 header bar on KDE/Xfce; LTS GTK lockout; no official Flatpak | Scrollback search arrived after 1.0 | ~60k stars; 1.3.1 Mar 2026 |
| WezTerm | Rust GPU; Linux X11 + Wayland, macOS, Windows, BSD | none | OpenGL/WebGpu; documented software front-end | unconfirmed; CRT WGSL PR #7649 unmerged | FreeType bitmap strikes by default | Tabs, splits, domains; `ActivateTab` by index; `ShowTabNavigator` overview | Partial: shipped in nightlies by Mar 2025 (#336 open); ssh latency #6806; windows land in the same tab row | Yes: built-in mux server | Lua, no GUI; auto reload | Official apt/Copr/AUR/Nix, Flatpak, AppImage, deb | Yes | ~28.5k stars; nightlies active |
| kitty | C/Python OpenGL; Linux X11 + Wayland, macOS | Author benchmark 134.55 MB/s vs gnome-terminal 61.83 | OpenGL; no fallback | Declined (#4842: "Not something I am interested in"); third-party CRTty preload | No: scalable only by design (#97, #1295) | Tabs, OS windows, layouts; `goto_tab N` unbound by default | Declined (#2170; FAQ: multiplexers "a bad idea") | No daemon (requests open) | `kitty.conf`; auto reload | Install script + distro; no official Flatpak | Scrollback search via kitten | ~34.6k stars; 0.48.2 Jul 2026 |
| Alacritty | Rust OpenGL; BSD/Linux/macOS/Windows | "the fastest terminal emulator in existence" | OpenGL ES 2.0; no fallback | No (#7520 unimplemented) | Rough: bitmap at largest strike | None by design (multi-window only) | Requested 2019 (#2410), "no desire"; open | No | TOML; live reload | Source build primary; community Snap | No search, no scrollbar by design | ~65.5k stars; 0.17.0 Apr 2026 |
| Rio | Rust wgpu; macOS, Linux, Windows, BSD, Steam Deck | "stable framerates" | Vulkan/wgpu; "minimal GPU" mode | Yes: RetroArch `.slangp` shaders via config (not on GL) | unconfirmed; #971 open for bitmap | Tabs, split panes | not found | unconfirmed | `config.toml`, live reload | Flatpak, distro, Cargo | Partial | ~7.4k stars; active |
| foot | C, Wayland only | "fast", in-repo benchmarks | CPU rendered | unconfirmed | Yes: "all fonts loadable by freetype, including bitmap fonts" | No tabs; server/footclient | No | Partial: server outlives client | `foot.ini` | Wayland only; widely packaged | URL hints | ~2.1k (Codeberg) |
| Contour | C++ OpenGL 3.3 or software; Linux, macOS, BSD, Windows | Declines the "fastest" framing | Documented software rasterizer fallback | unconfirmed | not documented | Tabs, splits | Own daemon "interoperates with tmux in both directions" | Yes: daemon survives window closure | YAML, runtime reload | Fedora/Arch/Void/openSUSE, Flathub, AppImage | Yes | ~3k stars |
| Warp | Rust GPU; macOS, Linux, Windows | none | GPU | unconfirmed | unconfirmed | Blocks, tabs, splits | Not -CC; own SSH extension | unconfirmed | GUI settings | deb, rpm, Arch, AppImage | Yes | AI features, accounts; 64.4k stars after open-sourcing May 2026 |
| Tabby | Electron (xterm.js) | none | Canvas fallback from WebGL | none | unconfirmed | Tabs, splits, quake-style | #5030, #2715 open | Restart state only | YAML + GUI; live reload | AppImage, tarball | Yes | Electron; ~74.1k stars |
| Hyper | Electron (React) | none | none | Yes via plugins (`cool-retro-hyper`: bloom, burn-in, jitter, curvature, scanlines) | unconfirmed | Tabs; splits via plugins | Declined (#2978, "no public spec"); own headless mux | No | `~/.hyper.js` + GUI; hot reload | deb, rpm, AppImage, Snap | Partial | Electron; last release Jul 2024 |
| Windows Terminal | Win32/WinUI, Windows only | "fast and efficient" | Direct3D | Yes: `experimental.retroTerminalEffect` (glow, scanlines); custom HLSL | DirectWrite outline | Tabs, splits, palette, jump-to-tab-N | #3656, #5612 open | App-state tabs | GUI + `settings.json`; live reload | N/A | Yes | ~104.7k stars |
| iTerm2 | Cocoa, macOS only | none | unconfirmed | none | Core Text outline | Tabs, splits, Cmd+number | Yes: the reference; windows ↔ tabs, splits ↔ splits; tmux Dashboard | Yes, via control mode | GUI Profiles + `.plist` | N/A | Yes (the incumbent's accumulated conveniences) | Memory leaks and AI features complained of |
| Konsole | Qt/KF6; Linux X11 + Wayland | unconfirmed | unconfirmed | none | unconfirmed | Tabs, splits, bookmarks, layouts | KDE bug 372496 open | Layout restore only | GUI dialog + profiles | Flathub, distro | Yes | KDE |
| GNOME Terminal / Ptyxis | GTK3+VTE / GTK4+VTE (Fedora 41, Ubuntu 25.10 default) | unconfirmed | unconfirmed | none | Bitmap disabled by default on Ubuntu; Ptyxis "thin" fonts complained of | Tabs; Ptyxis tab overview with previews | No | Pinned-tab context across restart | GUI + gsettings / profiles | Distro default; Flathub | Yes | VTE slow in throughput benchmarks |
| Zed terminal | Rust GPUI: Metal/Vulkan/DX11 | ~2 ms, 120 fps (blog) | Refuses llvmpipe by default | none | unconfirmed | Tabs in panel, splits | Rejected embedding tmux (#50584), proposes own daemon | None shipped | `settings.json` | Installer, unofficial apt/Flathub | Partial | ~89k stars (the editor) |
| Wave Terminal | Electron + Go | none | none | none | unconfirmed | Drag-and-drop blocks | Own block model | Local blocks not durable; ssh durable | GUI + CLI | Tarball | Yes | Electron |
| Zellij | Rust, inside any terminal | N/A | none | N/A | N/A | Tabs, panes, floating, stacked; session manager | #3965 open | Detach/reattach plus Session Resurrection | KDL | Distro, binaries, cargo | N/A | ~35.1k stars |
| tmux | C, inside any terminal | N/A | none | N/A | N/A | Sessions, windows, panes; prefix+number: the reference numbered model | The reference: `-C`/`-CC`, documented protocol | The reference: detach/reattach | `tmux.conf` | Distro | N/A | ~48k stars; 3.7c Jul 2026 |

Tool links: github.com/Swordfish90/cool-retro-term; ghostty.org; wezterm.org; sw.kovidgoyal.net/kitty; alacritty.org; rioterm.com; codeberg.org/dnkl/foot; contour-terminal.org; warp.dev; tabby.sh; hyper.is; github.com/microsoft/terminal; iterm2.com/documentation-tmux-integration.html; konsole.kde.org; gitlab.gnome.org/chergert/ptyxis; zed.dev/docs/terminal; waveterm.dev; zellij.dev; github.com/tmux/tmux.

## Field supply by pain

How much of the field serves each pain, and how.

**P1, latency.** Every GPU terminal claims it; kitty and Zed publish numbers, Alacritty claims "fastest" without them, Contour declines the framing. The corpus disputes all of it (a benchmark showing Ghostty worst on input latency; users going back to Terminal.app). No surveyed terminal publishes keystroke-to-glyph latency under visual effects; the CRT product's latency is described as "unbearable" and has no published figure.

**P2, GPU cost and breakage.** WezTerm and Contour are the two GPU terminals that document a software rendering fallback; the rest require a working GPU stack and say nothing about VMs or VNC. The CRT product's idle GPU draw and Wayland crashes are open issues. Zed refuses an emulated GPU by default.

**P3, the retro look.** Built-in, configurable CRT presets are documented in one dedicated desktop terminal (cool-retro-term), one Electron plugin set (Hyper), and one experimental flag (Windows Terminal). Ghostty, Rio and (by open PR) WezTerm let a user bring a shader; kitty declined. Phosphor persistence with a decaying ghost, a degauss on switch, or any effect beyond the in-glass post-process is documented in none of the surveyed products. A chassis, bezel or cabinet drawn around the glass is documented in none of the surveyed products (a Hyper plugin offers an optional monitor frame).

**P4, fonts.** foot is the one product whose docs promise bitmap strikes; WezTerm loads them by default; kitty refuses them by design; Ghostty, Alacritty and Rio carry open or contested issues. Blur at fractional scaling is open across the field (Ghostty #1938 closed after long debugging, kitty #7513 disputed, WezTerm #5149 open). A bitmap face drawn from its embedded strike at an integer multiple, never re-rasterised, is documented in none of the surveyed products. Ligatures are served by kitty, WezTerm, Ghostty; refused by Alacritty and foot.

**P5, many sessions.** Tabs are universal outside Alacritty and foot. Numbered-slot chords are default-bound in tmux, Zellij, Ghostty and Windows Terminal; a tab overview exists in WezTerm, Ptyxis and Windows Terminal's palette. A persistent numbered strip visible without a tab bar, with digit chords to jump and to move, is tmux's model; no surveyed GUI terminal draws one as chrome outside the text area.

**P6, tmux control mode.** iTerm2 is the reference and, on macOS, the only complete one. WezTerm ships control mode in nightlies (March 2025 onward) with an open ssh-latency bug and windows landing in the same tab row. Ghostty's #1935 is open at 731 reactions with a parser begun. Alacritty, kitty and Hyper declined; Tabby, Windows Terminal, Konsole and Zellij carry open requests. On Linux, the surveyed field offers one partial native implementation (WezTerm nightly).

**P7, survival.** tmux and Zellij (resurrection across reboot) are the reference; WezTerm's mux server and Contour's daemon are the independent mechanisms. No surveyed GUI terminal recovers a session's windows and screens from a tmux server after the terminal itself dies, without a daemon of its own, except through iTerm2's control mode on macOS.

**P8, configuration.** A text file with live reload is the native default (Ghostty, WezTerm, kitty, Alacritty, Rio, foot, Contour); GUI settings are the desktop and Electron default. A config written as a diff against a named preset, so a file stays short and means the same on another machine, is claimed by none of the surveyed products; Ghostty's `+show-config --default` dump comes closest. Hot reload on save is common; a parse error keeping the last good state is documented by few.

**P9, Linux fit.** The GTK4 terminals pay the desktop-consistency cost on KDE and Xfce; the Qt and Rust ones less. Wayland-only (foot) and X11-only (cool-retro-term) both draw complaints. The CRT product has had an open distro-packaging request since 2014.

**P10, table stakes.** The incumbents (iTerm2, Konsole, GNOME Terminal) have them; new GPU terminals ship without scrollback search, scrollbars, or URL hints and add them later under complaint.

**P11, weight.** Native Rust, C and Zig terminals escape it; the Electron trio, Warp's AI and accounts, and iTerm2's memory and AI features carry it.

## Cross-segment note

No surveyed product is built on a hardware metaphor beyond the glass. The CRT effect in every surveyed product is a flat post-process over the text area; no surveyed product maps the pointer back through the curvature, draws a cabinet, or degausses on a switch. Those absences are what `status.md` rates against.
